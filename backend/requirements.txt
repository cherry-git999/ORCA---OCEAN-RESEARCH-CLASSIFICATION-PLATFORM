# ORCA Multimodal Underwater Intelligence Platform — Backend Production Requirements
# Python 3.10+ / 3.12 compatible

# Core Web API & Server
fastapi>=0.110.0,<=0.142.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
python-multipart>=0.0.9

# Deep Learning & Computer Vision
# Note: For CUDA-accelerated GPU inference on Linux, install the matching PyTorch wheel:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# Standard pip install will default to standard wheels with CPU/CUDA fallback.
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.1.0
opencv-python-headless>=4.8.0
pillow>=10.0.0

# Automatic Visual Router & Scientific Computing
scikit-learn>=1.4.0
joblib>=1.3.0
numpy>=1.24.0
